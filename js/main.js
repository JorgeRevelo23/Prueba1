// Declaración de una variable
let nombre = "Jorge";

// Impresión en la consola
console.log("Hola-Rama 1 " + nombre + "!");

// Función que devuelve el cuadrado de un número
function calcularCuadrado(numero) {
  return numero * numero;
}

// Llamada a la función y almacenamiento del resultado en una variable
let resultado = calcularCuadrado(5);

// Impresión del resultado en la consola
console.log("El cuadrado de 5 es: " + resultado);
